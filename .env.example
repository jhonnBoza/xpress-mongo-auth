PORT=3000
MONGO_URI=mongodb+srv://<db_username>:<db_password>@dawacluster.tyrj3qk.mongodb.net/my_cloud_database?retryWrites=true&w=majority

# Opcional: otra base distinta a my_cloud_database (por defecto el código ya usa my_cloud_database).
# MONGO_DB_NAME=otra_base

# Local Windows: if Atlas fails with querySrv ECONNREFUSED, set in .env:
# USE_PUBLIC_DNS=1
